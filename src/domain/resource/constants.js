export const resourceProductTypes = Object.freeze({
  RENT: 'rent',
  EXTRA: 'extra',
});

export const resourcePriceTypes = Object.freeze({
  HOURLY: 'hourly',
  DAILY: 'daily',
  WEEKLY: 'weekly',
  FIXED: 'fixed',
});

export const productPriceType = Object.freeze({
  PER_PERIOD: 'per_period',
  FIXED: 'fixed',
});

export const resourcePeriodLengthType = Object.freeze({
  WITHIN_DAY: 'within_day',
  WHOLE_DAY: 'whole_day',
  OVERNIGHT: 'over_night',
});
